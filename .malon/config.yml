# Malon configuration — local-only mode (auto-configured)
local:
  enabled: true
  model: llama3.1-8b
  ollama_url: http://127.0.0.1:11434

pricing:
  last_verified: '2026-07-20'
  providers:
    ollama:
      llama3.1-8b:
        input_per_million: 0
        output_per_million: 0

search:
  provider: ollama
  model: llama3.1-8b
  subagent_timeout_ms: 15000
  max_rounds: 4
  max_output_bytes: 32768

cost:
  ceiling_usd: 0
  shadow:
    tokens_per_file_read: 4000
    avg_tokens_per_file: 350

rate_limits:
  max_calls_per_session: 100
  max_tokens_per_session: 500000
  window_ms: 60000

log:
  level: info
  file: ''

telemetry:
  enabled: false
