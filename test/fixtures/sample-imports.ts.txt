import { greet, User } from './sample.js';
import * as fs from 'node:fs';

export async function run(): Promise<void> {
  const u: User = { name: 'Malon', age: 1 };
  console.log(greet(u));
  fs.readFileSync('/dev/null');
}
